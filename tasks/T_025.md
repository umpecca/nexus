# T_025: Remove unused HorizonJS/ExtJS vendored dependency

**Status:** Done

**Task Description:** Remove the unused HorizonJS/ExtJS dependency from the project so the app tree contains only assets and vendored code that Nexus actually uses.

## Acceptance Criteria

1. `vendor/horizonjs` is removed.
2. `public/vendor/horizonjs` is removed.
3. Empty parent vendor folders are removed when no other assets remain.
4. The application has no remaining HorizonJS or ExtJS references outside historical task notes or generic text.
5. Build and tests still pass after the cleanup.

## Implementation Notes

- Removed the vendored HorizonJS source tree.
- Removed the generated/public HorizonJS assets.
- Confirmed `package.json` did not contain an npm HorizonJS or ExtJS package dependency.

## Verification

- Run a repository search for HorizonJS/ExtJS references.
- Run `npm run build`.
- Run `npm run test`.
