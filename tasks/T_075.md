# T_075: Add settings reset to defaults

## Status

Done

## Summary

Add a Settings dialog action that resets the current OS profile's editor preferences to the default Nexus values.

## Implementation Notes

- Added a `resetSettings(profileName)` helper that removes profile-scoped settings storage when available.
- Added a `Reset defaults` outline button to the Settings dialog footer.
- Wired the reset action in the app shell to clear stored settings and restore in-memory defaults immediately.
- Kept the existing settings save flow responsible for persisting the resulting default preference state.

## Verification

- Unit tests cover profile-scoped settings reset behavior.
- Run `npm run test`.
- Run `npm run build`.
- Run `git diff --check`.
