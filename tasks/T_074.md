# T_074: Improve dark theme contrast and editor caret visibility

## Status

Done

## Summary

Refine the saved dark theme so the palette feels neutral, editor carets and selections are visible, and toolbar button icons remain legible.

## Implementation Notes

- Replaced the green-heavy dark palette with neutral app, surface, toolbar, and border colors.
- Added theme tokens for editor caret, selection, active line, toolbar icon, and active toolbar icon colors.
- Applied explicit caret, cursor, selection, active-line, and gutter colors to rich text and CodeMirror editor surfaces.
- Forced toolbar SVG strokes and non-empty fills to inherit the visible toolbar button color.

## Verification

- Run `npm run test`.
- Run `npm run build`.
- Run `git diff --check`.
