# T_136: Export static data-model diagrams

## Status

Done

## Summary

Render valid `sql sqlschema` fences as self-contained static SVG diagrams in HTML, PDF, and web
publishing exports; malformed or unsupported schema blocks remain SQL code fences.

## Verification

- Focused export tests cover fence matching, SVG output, and invalid-model fallback.
- `npm run build` passes and emits the schema host page.
