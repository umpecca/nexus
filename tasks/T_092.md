# T_092: Move editor zoom controls to the View menu only

## Status

Done

## Summary

Remove zoom controls from the editor toolbar and keep zoom available from the native View menu, with the current zoom percentage visible in the menu.

## Implementation Notes

- Removed the toolbar zoom button group and related toolbar props/styles.
- Kept renderer-local zoom state and keyboard shortcuts unchanged.
- Published the current zoom percentage through the existing menu state bridge.
- Updated the Electron Reset Zoom menu item to include the current percentage.

## Verification

- Run `node --check electron/main.cjs`.
- Run `node --check electron/preload.cjs`.
- Run `npm run build`.
- Run `npm run test`.
- Run `git diff --check`.
