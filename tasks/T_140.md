# T_140: Combine Math toolbar actions

## Status

Done

## Summary

Replace the separate inline-math and display-math toolbar buttons with one Math dropdown, matching the
existing Diagram toolbar control.

## Acceptance Criteria

1. The toolbar shows one Math control instead of separate inline and display math buttons.
2. The Math dropdown offers **Inline math** and **Block math**.
3. Inline math continues to wrap the current selection or insert the default inline placeholder.
4. Block math continues to insert the existing Math/LaTeX code block.
5. Automated coverage verifies the grouped toolbar contract.

## Completion Notes

- Replaced the two button controls with `ButtonOrDropdownButton`.
- Retained the existing inline and display insertion behaviors behind explicit dropdown choices.
- Added a source contract test for the one-control/two-choice toolbar structure.

## Verification Results

- `npm run test -- src/components/editor/InsertKatexBlock.contract.test.ts`: passed.
- `npm run lint`: passed.
- `npm run build`: passed.
