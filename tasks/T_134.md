# T_134: Add the offline data-model editor

## Status

Done

## Summary

Add a dedicated Electron/Vite React Flow editor with table and column inspectors, FK connections,
auto-layout, undo/redo, PostgreSQL source preview, and copy/download SQL output.

## Verification

- Focused IPC normalization tests pass.
- `npm run build` emits `dist/sqlschema-host.html`.
