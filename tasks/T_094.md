# T_094: Restore the pre-print-preview PDF export flow

## Status

Done

## Summary

Revert File/Export as PDF away from the print-preview-era retry/fallback pipeline and back to the earlier direct rich export flow.

## Implementation Notes

- Restored the direct PDF handler shape from before print preview work: render export HTML, prompt for a PDF path, load the rendered HTML into one hidden BrowserWindow, render Mermaid, wait for fonts, then call `printToPDF`.
- Removed the preview-era CSS page-size print path, retry chain, off-screen retry, temporary export HTML files, and text-first fallback path from live PDF export.
- Kept newer export options for selected font family, base font size, paragraph spacing, paper size, paper orientation, and page margins.
- Kept PDF failure visible through the existing export error dialog rather than creating a plain text fallback PDF.

## Verification

- Run `node --check electron/main.cjs`.
- Run `node --check electron/preload.cjs`.
- Run `npm run build`.
- Run `npm run test`.
- Run `git diff --check`.
